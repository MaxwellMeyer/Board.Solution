@{
  Layout = "_Layout";
}

@using Board.Models;
@model List<Board.Models.Post>;

<h1>Posts</h1>

@if (@Model.Count == 0)
{
  <h3>No posts have been added yet!</h3>
}

@foreach (Post post in Model)
{

}


<p>@Html.ActionLink("Add new post", "Create") @Html.ActionLink("Home", "Index", "Home")</p>